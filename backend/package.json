{
  "name": "@runesmith/backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "set PYTHONIOENCODING=utf-8 && set PYTHONUTF8=1 && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 8000"
  }
}
